---
title: Yieldless
description: Tuple-first TypeScript primitives for error handling, cancellation, resources, and platform integration.
template: splash
hero:
  tagline: Fewer moving parts. Better async code.
  actions:
    - text: Read the quickstart
      link: /yieldless/guides/quickstart/
      icon: right-arrow
    - text: Browse the reference
      link: /yieldless/reference/error/
      icon: open-book
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

<div class="yl-band">
  <div class="yl-lead">
    Yieldless gives you the parts people actually reach for in large TypeScript
    backends, but it keeps them in ordinary JavaScript: tuples instead of
    wrapper classes, <code>AbortSignal</code> instead of fibers, and
    <code>await using</code> instead of a proprietary scope runtime.
  </div>
  <ul class="yl-tag-list">
    <li class="yl-tag">Tuple-based errors</li>
    <li class="yl-tag">Abort-aware concurrency</li>
    <li class="yl-tag">Node and Electron adapters</li>
    <li class="yl-tag">Zero runtime dependencies</li>
  </ul>
</div>

## The shape of the library

Yieldless is intentionally split into two layers.

- The core layer covers tuple errors, structured concurrency, resource cleanup,
  and dependency injection.
- The platform layer adds retry logic, async context, validation adapters,
  routing helpers, Electron IPC, and Node wrappers.

Nothing in the library asks you to stop writing normal async functions. The goal
is to make common failure-heavy code paths easier to reason about without
changing the runtime beneath your feet.

```ts
import { safeTry } from "yieldless/error";
import { runTaskGroup } from "yieldless/task";

const [repoError, repo] = await safeTry(loadRepository(repoId));

if (repoError) {
  return [repoError, null] as const;
}

return await runTaskGroup(async (group) => {
  const refs = group.spawn((signal) => loadRefs(repo.path, signal));
  const status = group.spawn((signal) => loadStatus(repo.path, signal));

  return {
    repo,
    refs: await refs,
    status: await status,
  };
});
```

## What Yieldless optimizes for

<div class="yl-advice-grid">
  <div class="yl-advice-card do">
    <strong>Native control flow</strong>
    <div class="yl-copy">
      The library composes around <code>Promise</code>,
      <code>AbortController</code>, <code>AsyncLocalStorage</code>, and
      <code>Symbol.asyncDispose</code> instead of standing up a separate
      scheduler.
    </div>
  </div>
  <div class="yl-advice-card do">
    <strong>Visible failure paths</strong>
    <div class="yl-copy">
      If something can fail, the tuple is right there in front of you. There is
      no hidden exception channel unless you choose to rethrow.
    </div>
  </div>
  <div class="yl-advice-card do">
    <strong>Incremental adoption</strong>
    <div class="yl-copy">
      You can use one module at a time. There is no requirement to wrap the
      entire application in a framework-specific runtime.
    </div>
  </div>
</div>

## Read the docs by problem

<CardGrid stagger>
  <Card title="I want to start fast" icon="rocket">
    Go to the [quickstart](/yieldless/guides/quickstart/) for the shortest route from
    install to working code.
  </Card>
  <Card title="I want the design rules" icon="pencil">
    Read [Design Rules](/yieldless/guides/design-rules/) for the philosophy behind the
    API surface and where the sharp edges are on purpose.
  </Card>
  <Card title="I want practical boundaries" icon="warning">
    Read [Do and Don't](/yieldless/guides/do-and-dont/) for the patterns worth repeating
    and the ones that usually make tuple-based code worse.
  </Card>
  <Card title="I need module details" icon="document">
    The [reference section](/yieldless/reference/error/) covers every exported module with
    signatures, examples, and rules of thumb.
  </Card>
  <Card title="I want end-to-end examples" icon="open-book">
    The [recipes](/yieldless/recipes/resilient-service-flow/) show how the small pieces
    combine in a service and in an Electron app.
  </Card>
</CardGrid>

## Module map

<CardGrid>
  <Card title="Core primitives" icon="document">
    `yieldless/error`, `yieldless/task`, `yieldless/resource`,
    and `yieldless/di`
  </Card>
  <Card title="Operational tools" icon="setting">
    `yieldless/retry`, `yieldless/context`, `yieldless/all`,
    and `yieldless/schema`
  </Card>
  <Card title="Application boundaries" icon="open-book">
    `yieldless/router`, `yieldless/ipc`, and `yieldless/node`
  </Card>
</CardGrid>

## What it is not

<div class="yl-note">
  <div class="yl-copy">
    Yieldless is not trying to replace the JavaScript runtime. It does not ship
    fibers, effect interpreters, global service containers, or a replacement
    promise implementation. If a native platform feature is already good enough,
    Yieldless wraps it instead of competing with it.
  </div>
</div>
